---
description: 开发 Server API 端点时的规范
globs: packages/server/server/routes.py,packages/server/server/models.py,packages/server/server/task_manager.py
alwaysApply: false
---

# API 开发规范

## 路由定义

- 所有路由在 `routes.py` 中定义，使用 `APIRouter`
- 路径前缀 `/api/`，资源名使用复数形式
- 请求/响应模型定义在 `models.py`，使用 Pydantic BaseModel

## 错误处理

- 业务错误返回 `HTTPException`，带明确的 `detail` 消息
- 在 `except` 子句中使用 `raise HTTPException(...) from err`
- 404：资源不存在，400：参数错误，500：服务器内部错误

## 任务操作

- 任务相关操作通过 `TaskManager` 单例执行，不要直接操作数据库
- 新增任务操作需要：
  1. 在 `TaskManager` 中添加方法
  2. 在 `routes.py` 中添加路由
  3. 在 `models.py` 中添加请求/响应模型
  4. 更新 Web 前端的 `api/client.ts` 和 `api/types.ts`

## 数据库

- 使用 `get_session()` 上下文管理器获取数据库会话
- ORM 模型定义在 `db_models.py`
- 新增字段需要在 `database.py` 的 `_migrate()` 中添加迁移逻辑

## SSE

- SSE 事件通过 `TaskManager._notify()` 推送
- 事件格式：`{"type": "...", "data": {...}}`
- 前端通过 `EventSource` 连接 `/api/tasks/{id}/events`
