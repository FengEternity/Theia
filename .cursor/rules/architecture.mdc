---
description: Theia 项目架构规范
globs: packages/**/*
alwaysApply: false
---

# 架构规范

## 包依赖方向

```
web → server → agent → (LLM/MinerU/Remotion)
```

- `agent` 包不依赖 `server`，仅通过 `ProgressCallback` 协议回调
- `server` 依赖 `agent`（作为 editable path dependency）
- `video` 包独立运行，由 agent 的 `renderer.py` 通过子进程调用

## Agent 层

- 流水线状态通过 `PipelineState`（TypedDict）传递，扁平结构
- 进度回调通过模块级 `_callback_registry` 按 workspace 查找，不存入 state
- 新增流水线步骤需要：更新 `pipeline.py` 的 StateGraph + 添加对应处理模块
- 缓存使用 `cache.py` 的 `get_cached/set_cached`，基于文件内容哈希

## Server 层

- `TaskManager` 是全局单例，管理所有任务生命周期
- 任务在后台线程中运行 agent pipeline
- SSE 通过 asyncio.Queue 推送事件
- 数据库操作使用 `database.get_session()` 上下文管理器

## Video 层

- 每个场景组件接收 `SceneData` props，包含 narration、visual_elements 等
- 通用组件放在 `components/`，场景组件放在 `scenes/`
- 多尺寸适配通过 `useScale` hook
- VideoScript 的 Zod schema（`types/script.ts`）需要与 Python 端 Pydantic 模型保持同步

## Web 层

- API 调用封装在 `api/client.ts`，类型定义在 `api/types.ts`
- SSE 连接在 `TaskDetailView.vue` 中管理
- 路由定义在 `router/index.ts`
