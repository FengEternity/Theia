---
description: Python 代码风格规范（agent 和 server 包）
globs: packages/agent/**/*.py,packages/server/**/*.py
alwaysApply: false
---

# Python 代码规范

## 格式化与 Lint

- 项目使用 Ruff 进行 lint 和格式化，配置在根目录 `ruff.toml`
- 行宽 120 字符，使用双引号，空格缩进
- 修改 Python 文件后运行 `ruff check --fix` 和 `ruff format` 确认无问题

## 数据模型

- 所有数据模型继承 `pydantic.BaseModel`，字段使用 `Field(description="...")` 注明含义
- Agent 和 Server 之间通过 `ProgressCallback` 协议通信，不要直接导入 server 模块到 agent
- 类型注解使用 `from __future__ import annotations` 开启延迟解析

## LLM 调用

- 所有 LLM 调用必须通过 `theia.llm_client.robust_completion()`，不要直接调用 `litellm`
- 模型名称通过 `LLMConfig` 传递，不要硬编码模型名

## 错误处理

- 在 `except` 子句中使用 `raise ... from err` 保留异常链
- FastAPI 路由中的异常统一转为 `HTTPException`

## 导入顺序

- 标准库 → 第三方 → 本项目（由 Ruff isort 自动管理）
- `theia` 和 `server` 作为 first-party 包

## 日志

- 使用 `logging.getLogger(__name__)` 创建 logger
- 不要在生产代码中使用 `print()`（CLI 入口除外）
