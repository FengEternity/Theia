---
description: 修改 Agent 流水线时的规范
globs: packages/agent/theia/pipeline.py,packages/agent/theia/extractor.py,packages/agent/theia/scriptwriter.py,packages/agent/theia/parser.py,packages/agent/theia/tts.py,packages/agent/theia/renderer.py
alwaysApply: false
---

# 流水线开发规范

## 状态管理

- `PipelineState` 使用 TypedDict + `total=False`，所有字段可选
- 新增状态字段必须在 `PipelineState` 中声明类型
- 不可序列化的对象（如回调）不要放入 state
- 节点函数签名：`def node_name(state: PipelineState) -> dict`，返回需要更新的字段

## 进度通知

- 通过 `_callback_registry` 获取回调，用 `_notify()` 推送 StepInfo
- StepInfo 包含：step（1-5）、name、status、message、progress_pct
- 状态流转：started → progress（可选，多次） → completed / failed

## 交互模式

- 使用 `langgraph.types.interrupt()` 暂停等待用户
- 恢复使用 `Command(resume=edited_data)` 传入用户编辑的数据
- 检查 `state.get("interactive_mode")` 决定是否暂停

## 缓存

- 解析结果和提取结果支持缓存
- 缓存 key 基于输入内容的 SHA256 哈希
- 修改提取逻辑后考虑是否需要失效旧缓存

## 错误处理

- 节点中的异常会被 LangGraph 捕获
- 设置 `state["error"]` 传递错误信息
- 使用 `logger.exception()` 记录完整堆栈
